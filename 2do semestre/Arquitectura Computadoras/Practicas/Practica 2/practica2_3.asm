ORG 1000H
	MAYUS DB 41H
	MINUS DB 61H
	STR DB ?
ORG 2000H
	MOV BX, OFFSET STR
LOOP: CMP MAYUS, 5BH
	JZ FIN_LOOP
	MOV AL, MAYUS
	MOV [BX], AL
	INC MAYUS
	INC BX
	MOV AL, MINUS
	MOV [BX], AL
	INC MINUS
	INC BX
	JMP LOOP
FIN_LOOP: MOV AX, BX
	MOV BX, OFFSET STR
	SUB AX, BX
	INT 7
	INT 0
END
